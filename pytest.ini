[pytest]
# Put the repository root on sys.path so `from app...` imports resolve when
# tests are run as bare `pytest`. Without this, only `python -m pytest` works
# (that form adds the working directory itself), which silently breaks a fresh
# clone and CI.
pythonpath = .
testpaths = tests
